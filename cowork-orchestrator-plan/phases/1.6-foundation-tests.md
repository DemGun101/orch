# 1.6 — Foundation Unit Tests

```
Continuing "cowork-orchestrator". All foundation code is implemented. Now write unit tests using vitest. Create a mock agent helper first, then test each core module.

## 1. tests/fixtures/mock-agent.ts — Mock Agent for Testing

Create a `MockAgent` class extending BaseAgent:
- Constructor takes partial AgentConfig with defaults
- `execute(task)` returns a successful TaskResult with `{ output: { result: 'mock response for ' + task.name }, duration: 100 }`
- Optional: accept a custom execute function in constructor for testing different behaviors (success, failure, timeout)
- Also create `createMockAgentConfig(overrides?)` helper that returns a valid AgentConfig with sensible defaults

## 2. tests/unit/persistence.test.ts

Test PersistenceLayer using in-memory SQLite (pass ':memory:' as db path):
- Test initialize() creates all tables
- Test saveTask / getTask / updateTask / listTasks roundtrip
- Test saveCheckpoint / getLatestCheckpoint
- Test appendAuditLog / queryAuditLog with filters
- Test close() works without error
- Test error handling for invalid data

## 3. tests/unit/shared-memory.test.ts

Test SharedMemoryStore:
- Test set/get/delete with namespaces
- Test getNamespace returns all keys
- Test subscribe fires callback on matching changes
- Test subscribe does NOT fire on non-matching changes

## 4. tests/unit/message-bus.test.ts

Test MessageBus:
- Test subscribe + publish delivers to correct agent
- Test broadcast (to='*') delivers to all subscribers on channel
- Test request/response with correlationId matching
- Test request timeout rejects the promise
- Test dead letter queue receives undeliverable messages
- Test getMetrics returns correct counts

## 5. tests/unit/agent-registry.test.ts

Test AgentRegistry (use MockAgent):
- Test register/unregister/get
- Test findByCapability returns correct agents
- Test findBestMatch considers capability + load + error rate
- Test getAvailable excludes offline and overloaded agents
- Test getMetrics

## 6. tests/unit/task-manager.test.ts

Test TaskManager:
- Test createTask generates UUID and sets defaults
- Test submitTask + getNextTask returns highest priority
- Test priority ordering: critical > high > medium > low
- Test dependency resolution: task with unmet deps is NOT returned by getNextTask
- Test dependency resolution: task IS returned once all deps are completed
- Test decompose creates subtasks with correct parentId
- Test cancelTask cascades to subtasks
- Test onTaskComplete marks parent complete when all subtasks done

## 7. tests/unit/workflow-engine.test.ts

Test WorkflowEngine (use MockAgent):
- Test validateDAG detects cycles and rejects
- Test validateDAG accepts valid DAG
- Test topologicalSort returns correct order
- Test execute runs nodes in correct dependency order
- Test parallel execution: independent nodes run concurrently (check timing)
- Test conditional edges: edge with condition=false is skipped
- Test cancel stops pending nodes
- Test context passing: output of node A is available to node B

Run `npm test` — all tests must pass. Fix any failures.

Then commit: "feat: implement core foundation — task manager, agent registry, message bus, workflow engine"
```
