# 2.2.5 — Migrate from Anthropic SDK to Gemini (OpenAI-compatible)

> **IMPORTANT: Run this BEFORE 2.3. This refactors all existing code from Anthropic SDK to Gemini via the OpenAI SDK.**

```
We need to migrate the "cowork-orchestrator" project from the Anthropic SDK to Google Gemini, using the OpenAI-compatible endpoint. This is a targeted refactor — the architecture stays the same, only the LLM client layer changes.

## 1. Swap Dependencies

- Run: npm uninstall @anthropic-ai/sdk
- Run: npm install openai

## 2. Update .env.example

Replace:
  ANTHROPIC_API_KEY=your-key-here
With:
  GEMINI_API_KEY=your-gemini-key-here
  LLM_BASE_URL=https://generativelanguage.googleapis.com/v1beta/openai/
  LLM_MODEL=gemini-2.0-flash

Also create/update .env file with:
  GEMINI_API_KEY=AIzaSyBP5HPYEYPoub_keJjAj2y2GDjSV-SLBR0
  LLM_BASE_URL=https://generativelanguage.googleapis.com/v1beta/openai/
  LLM_MODEL=gemini-2.0-flash

## 3. Create src/llm/client.ts — LLM Client Abstraction

Create a new folder src/llm/ and file client.ts:

```typescript
import OpenAI from 'openai';
import dotenv from 'dotenv';
dotenv.config();

export interface LLMConfig {
  apiKey?: string;
  baseURL?: string;
  model?: string;
}

export function createLLMClient(config?: LLMConfig): OpenAI {
  return new OpenAI({
    apiKey: config?.apiKey || process.env.GEMINI_API_KEY || '',
    baseURL: config?.baseURL || process.env.LLM_BASE_URL || 'https://generativelanguage.googleapis.com/v1beta/openai/',
  });
}

export function getDefaultModel(config?: LLMConfig): string {
  return config?.model || process.env.LLM_MODEL || 'gemini-2.0-flash';
}

// Re-export OpenAI types for convenience
export type { OpenAI };
export type ChatMessage = OpenAI.ChatCompletionMessageParam;
export type ChatResponse = OpenAI.ChatCompletion;
export type ChatTool = OpenAI.ChatCompletionTool;
```

## 4. Update src/core/types.ts

- Change AgentConfig.model default comment from 'claude-sonnet-4-20250514' to 'gemini-2.0-flash'
- Add to OrchestratorConfig:
  llm?: { apiKey?: string; baseURL?: string; model?: string; }

## 5. Update src/agents/claude-agent.ts → Rename to src/agents/llm-agent.ts

Rename the file and class:
- `ClaudeAgent` → `LLMAgent`
- Replace all `Anthropic` imports with `OpenAI` from 'openai'
- Replace the constructor: instead of taking an Anthropic client, take an OpenAI client
- Update `execute(task)`:
  - Replace `anthropic.messages.create()` with `client.chat.completions.create()`
  - Message format changes:
    - Anthropic: { role: 'user', content: 'text' }
    - OpenAI: { role: 'user', content: 'text' } (same format, convenient!)
  - System prompt: move from separate `system` param to a message with role: 'system'
  - Response parsing:
    - Anthropic: response.content[0].text
    - OpenAI: response.choices[0].message.content
  - Tool use:
    - Anthropic: response.content.find(b => b.type === 'tool_use')
    - OpenAI: response.choices[0].message.tool_calls
  - Token usage:
    - Anthropic: response.usage.input_tokens / output_tokens
    - OpenAI: response.usage.prompt_tokens / completion_tokens
  - Retry errors: catch errors with status 429/500/503 (Gemini equivalents)
- Update `chat()` similarly

- Keep the old file name as an alias: create a new claude-agent.ts that re-exports:
  export { LLMAgent as ClaudeAgent } from './llm-agent';
  This preserves backward compatibility.

## 6. Update src/agents/tool-agent.ts

- Import from './llm-agent' instead of Anthropic
- Update tool format for OpenAI SDK:
  Anthropic tools: { name, description, input_schema }
  OpenAI tools: { type: 'function', function: { name, description, parameters } }
- Update tool_use response parsing:
  Anthropic: content blocks with type='tool_use'
  OpenAI: message.tool_calls array with { id, type: 'function', function: { name, arguments } }
- Update tool_result sending:
  Anthropic: { type: 'tool_result', tool_use_id, content }
  OpenAI: { role: 'tool', tool_call_id, content }

## 7. Update src/intelligence/task-decomposer.ts

- Replace `Anthropic` client type with `OpenAI` from 'openai'
- Replace all `anthropic.messages.create()` calls with `client.chat.completions.create()`
- For structured output, use OpenAI function calling instead of Anthropic tool_use:
  tools: [{ type: 'function', function: { name: 'decompose_task', description: '...', parameters: { ... } } }]
  tool_choice: { type: 'function', function: { name: 'decompose_task' } }
- Parse response from: response.choices[0].message.tool_calls[0].function.arguments (JSON string, need JSON.parse)
- Update model references: 'claude-sonnet-4-20250514' → 'gemini-2.0-flash'

## 8. Update src/intelligence/agent-selector.ts

- Same pattern as task-decomposer: swap Anthropic → OpenAI
- Replace messages.create with chat.completions.create
- Use function calling for structured output
- Parse tool_calls[0].function.arguments
- Update model references

## 9. Update src/intelligence/conflict-resolver.ts

- Same swap: Anthropic → OpenAI client
- Same structured output via function calling
- Parse from tool_calls

## 10. Update src/core/orchestrator.ts

- Replace Anthropic client creation with OpenAI client via createLLMClient()
- Import { createLLMClient, getDefaultModel } from '../llm/client'
- Replace `setAnthropicClient()` with `setLLMClient(client: OpenAI)` if it exists
- When creating agents, pass the OpenAI client instead of Anthropic
- Update any model string references

## 11. Update src/index.ts exports

- Add: export { LLMAgent } from './agents/llm-agent'
- Keep: export { ClaudeAgent } from './agents/claude-agent' (backward compat alias)
- Add: export { createLLMClient, getDefaultModel } from './llm/client'

## 12. Update existing tests

- In tests/fixtures/mock-agent.ts: no changes needed (doesn't use LLM client directly)
- If any test imports Anthropic, switch to OpenAI mock
- Update mock response shapes from Anthropic format to OpenAI format:
  Anthropic: { id, type: 'message', role: 'assistant', content: [...], usage: { input_tokens, output_tokens } }
  OpenAI: { id, object: 'chat.completion', choices: [{ index: 0, message: { role: 'assistant', content: '...' }, finish_reason: 'stop' }], usage: { prompt_tokens, completion_tokens, total_tokens } }

## 13. Verify

1. Run `npx tsc --noEmit` — must compile with ZERO errors
2. Run `npm test` — all existing tests must pass
3. Fix any issues

Commit: "refactor: migrate from Anthropic SDK to Gemini via OpenAI-compatible endpoint"
```
