# 3.5 — Integration Tests

```
Continuing "cowork-orchestrator". Monitoring and examples are done. Now write integration tests. The project uses OpenAI SDK + Gemini — use mock-openai.ts for mocking.

## 1. tests/integration/workflow.test.ts — Workflow Integration Tests

Using the mock OpenAI client from tests/fixtures/mock-openai.ts and MockAgent from tests/fixtures/mock-agent.ts:

Test: "complete workflow executes all nodes in order"
- Create an OrchestrationEngine
- Register 2 mock agents
- Define a simple 3-node workflow: A → B → C
- Execute the workflow
- Verify all 3 nodes completed in order
- Verify WorkflowResult has correct outputs and timing

Test: "parallel nodes execute concurrently"
- Workflow: A → [B, C] → D (B and C are parallel)
- Verify B and C both ran
- Verify D ran after both B and C

Test: "workflow checkpoint and resume"
- Start a 4-node workflow
- After node 2, simulate a failure in node 3
- Verify checkpoint was saved after node 2
- Create a new engine, load checkpoint, resume
- Verify nodes 1-2 are skipped, node 3 retries, node 4 runs

Test: "conflict detection in parallel nodes"
- Two parallel nodes produce conflicting outputs
- Verify conflict is detected after both complete

## 2. tests/integration/tool-execution.test.ts — Tool Integration Tests

Test: "tool registration and execution"
- Create ToolRegistry, register a custom tool
- Create ToolExecutor
- Execute the tool with valid input
- Verify ToolResult is correct

Test: "safety sandbox blocks disallowed paths"
- Configure allowedPaths = ['/tmp/test']
- Try to read_file from '/etc/passwd'
- Verify it's blocked with an error

Test: "safety sandbox blocks dangerous commands"
- Configure blockedCommands = ['rm -rf', 'sudo']
- Try to execute_command 'rm -rf /'
- Verify it's blocked

Test: "tool timeout"
- Create a tool that takes 5 seconds
- Set timeout to 1 second
- Verify it times out with ToolResult.success = false

Test: "batch execution runs independent tools in parallel"
- Execute 3 tools via executeBatch
- Verify all 3 have results

## 3. Verify Everything

Run:
1. `npx tsc --noEmit` — must compile clean
2. `npm test` — ALL tests (unit + integration) must pass

Fix any issues.

Commit: "feat: implement integration layer — MCP tools, monitoring, dashboard, examples"
```
