# 2.4 — Intelligence Layer Tests

```
Continuing "cowork-orchestrator". All intelligence modules are implemented using the OpenAI SDK (pointing at Gemini). Now write tests. First create a mock OpenAI client, then test each module.

## 1. tests/fixtures/mock-openai.ts — Mock OpenAI Client

Create a `createMockOpenAI()` function that returns a mock OpenAI client:
- Has a `chat` property with a `completions` property with a `create` method
- `create()` returns predetermined responses based on the input
- Support configuring responses: `createMockOpenAI(responses?: Map<string, any>)`
- Default responses should include:
  - A decomposition response (for TaskDecomposer): returns 3 subtasks with dependencies
  - An agent selection response (for AgentSelector): picks first agent with confidence 0.85
  - A conflict detection response (for ConflictResolver): returns no conflicts
  - A quality assessment response (for QualityAssessor): returns score 85
- Each response should be shaped like a real OpenAI chat completion:
  {
    id: 'chatcmpl-mock',
    object: 'chat.completion',
    created: Date.now(),
    model: 'gemini-2.0-flash',
    choices: [{
      index: 0,
      message: {
        role: 'assistant',
        content: null,
        tool_calls: [{
          id: 'call_mock',
          type: 'function',
          function: { name: 'function_name', arguments: JSON.stringify({...}) }
        }]
      },
      finish_reason: 'tool_calls'
    }],
    usage: { prompt_tokens: 100, completion_tokens: 50, total_tokens: 150 }
  }

Also delete or update the old tests/fixtures/mock-anthropic.ts if it exists — replace with mock-openai.ts.

## 2. tests/unit/task-decomposer.test.ts

- Test decompose() returns correct subtask structure from mock response
- Test decompose() validates response with Zod (test with malformed response)
- Test estimateComplexity() returns valid complexity level
- Test suggestWorkflow() returns valid Workflow object

## 3. tests/unit/agent-selector.test.ts

- Test selectAgent() returns correct agent from mock response
- Test rankAgents() returns sorted list
- Test suggestTeam() assigns agents to all workflow nodes
- Test LRU cache: second call with same inputs returns cached result without API call
- Test cache expiry: after TTL, new API call is made

## 4. tests/unit/conflict-resolver.test.ts

- Test detectConflicts() with mock outputs that have no conflicts → empty array
- Test detectConflicts() with conflicting outputs → returns Conflict objects
- Test resolve() returns correct strategy (MERGE, PREFER, etc.)
- Test preventConflict() returns risk level and guardrails

## 5. tests/unit/quality-assessor.test.ts

- Test assess() returns valid QualityReport with scores
- Test threshold logic: critical task with score 85 fails (needs 90), medium task with score 85 passes (needs 70)
- Test compare() ranks results correctly
- Test validateWorkflowOutput() returns report

Run `npm test` — all tests must pass (both new and existing Phase 1 tests).

Commit: "feat: implement intelligence layer — AI decomposition, selection, conflict resolution, quality assessment (Gemini-powered)"
```
